export type Memoir = {
  id: number
  title: string
  content: string
  date: string // the editable date (ISO string)
  createdAt: string // when the memoir was first created
  updatedAt: string // last updated
}
